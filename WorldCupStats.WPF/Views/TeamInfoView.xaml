<Window x:Class="WorldCupStats.WPF.Views.TeamInfoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorldCupStats.WPF.Views"
        mc:Ignorable="d"
        Title="Team Information" 
        Height="450" Width="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation 
                        Storyboard.TargetProperty="Opacity"
                        From="0" To="1" Duration="0:0:0.5"/>
                    <DoubleAnimation 
                        Storyboard.TargetProperty="(Window.RenderTransform).(TranslateTransform.Y)"
                        From="50" To="0" Duration="0:0:0.5">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Window.RenderTransform>
        <TranslateTransform/>
    </Window.RenderTransform>

    <Border Background="#333333" 
            CornerRadius="15" 
            BorderBrush="#666666" 
            BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0">
                <TextBlock x:Name="txtTeamName" 
                           Text="Team Name" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>
                <TextBlock x:Name="txtFifaCode"
                           Text="FIFA Code: ABC" 
                           FontSize="16" 
                           Foreground="#CCCCCC"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>
            </StackPanel>

            <!-- Statistics -->
            <Grid Grid.Row="1" Margin="0,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Games Played:" Grid.Row="0" Grid.Column="0" Foreground="#CCCCCC" Margin="0,5"/>
                <TextBlock x:Name="txtGamesPlayed" Text="0" Grid.Row="0" Grid.Column="1" Foreground="White" Margin="0,5"/>

                <TextBlock Text="Wins:" Grid.Row="1" Grid.Column="0" Foreground="#CCCCCC" Margin="0,5"/>
                <TextBlock x:Name="txtWins" Text="0" Grid.Row="1" Grid.Column="1" Foreground="#90EE90" Margin="0,5"/>

                <TextBlock Text="Draws:" Grid.Row="2" Grid.Column="0" Foreground="#CCCCCC" Margin="0,5"/>
                <TextBlock x:Name="txtDraws" Text="0" Grid.Row="2" Grid.Column="1" Foreground="#FFD700" Margin="0,5"/>

                <TextBlock Text="Losses:" Grid.Row="3" Grid.Column="0" Foreground="#CCCCCC" Margin="0,5"/>
                <TextBlock x:Name="txtLosses" Text="0" Grid.Row="3" Grid.Column="1" Foreground="#FF6B6B" Margin="0,5"/>

                <Separator Grid.Row="4" Grid.ColumnSpan="2" Margin="0,10"/>

                <StackPanel Grid.Row="5" Grid.ColumnSpan="2">
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Goals Scored:" Foreground="#CCCCCC"/>
                        <TextBlock x:Name="txtGoalsScored" Text="0" Grid.Column="1" Foreground="#90EE90"/>
                    </Grid>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Goals Conceded:" Foreground="#CCCCCC"/>
                        <TextBlock x:Name="txtGoalsConceded" Text="0" Grid.Column="1" Foreground="#FF6B6B"/>
                    </Grid>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Goal Difference:" Foreground="#CCCCCC"/>
                        <TextBlock x:Name="txtGoalDifference" Text="0" Grid.Column="1" Foreground="White"/>
                    </Grid>
                </StackPanel>
            </Grid>

            <!-- Close Button -->
            <Button Grid.Row="2" 
                    Content="Close" 
                    Width="100"
                    Height="35"
                    Margin="0,20,0,0"
                    Background="#FF4B4B4B"
                    Foreground="White"
                    BorderThickness="0"
                    Click="CloseButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="17.5">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF6B6B6B"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window> 